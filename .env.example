# Auth stays OFF in this tree. Do not set true unless you are explicitly adding accounts.
VITE_AUTH_ENABLED=false

# Only needed if Auth is turned on.
# GROK_AUTH_ISSUER=
# GROK_AUTH_CLIENT_ID=
# GROK_AUTH_CLIENT_SECRET=
# BETTER_AUTH_URL=
# BETTER_AUTH_SECRET=

# Unowned control snapshot. Personal memory is never stored here.
# DATABASE_URL=
